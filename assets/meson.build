# glslSources = ['fragment.glsl', 'vertex.glsl']
# glslOutputs = []
# foreach s : glslSources
#     outfile = s.substring(0, -5)
#     glslOutputs += outfile
# endforeach

# glslc = find_program('glslc')

# custom_target('spirv', input: s, output: glslOutputs, command: [glslc, '@INPUT@'])
